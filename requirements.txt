# Ecosetu CBAM — runtime dependencies.
# Only these three are imported by the app at runtime; pip resolves the rest
# (numpy, pyarrow, altair, etc.) transitively. Pinned to the tested versions.
streamlit==1.56.0
pandas==3.0.2
fpdf2==2.8.7

# Dev-only (NOT needed to run the deployed app):
#   openpyxl, pytest  — used by scripts/ingest_cbam_defaults.py and tests/.
